# wiki-mcp

Read-only Model Context Protocol server that exposes an LLM-wiki vault (Karpathy pattern) to Claude clients. Zero-maintenance, runs free on Cloudflare Workers, fetches wiki content from a private GitHub repo on demand.

Full setup guide added in a later task.
